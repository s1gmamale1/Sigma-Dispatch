node_modules/
coverage/

# Local Claude Code / RuFlo project state — NOT part of the Sigma-Dispatch plugin
.claude/
.claude-flow/
.swarm/
*.db
*.pid

# Lane runtime (created at runtime inside the target repo)
**/worktrees/
.claude/lanes/
.codex-home/

# Local RuFlo project config (boilerplate, not part of the plugin)
CLAUDE.md

# Ship ONLY the plugin's own tests. RuFlo's testgaps hook keeps generating cruft
# tests for .claude/helpers/* — whitelist ours instead of blacklisting each new one.
tests/unit/*.test.js
!tests/unit/lanes.test.js
!tests/unit/statusline.test.js
!tests/unit/consult.test.js
tests/integration/*.test.js
!tests/integration/lanes-pipeline.test.js
